# Python dependencies for Kairos Lead Qualification System
# Install with: pip install -r requirements.txt

requests>=2.31.0          # HTTP client — direct Anthropic API calls + HubSpot/Slack webhooks
python-dotenv>=1.0.0      # Loads ANTHROPIC_API_KEY from .env for local development
fastapi>=0.100.0          # API framework — exposes /qualify and /health endpoints
uvicorn>=0.23.0           # ASGI server — local development only (production runs on Vercel)
pydantic>=2.0.0           # Request/response validation for FastAPI
